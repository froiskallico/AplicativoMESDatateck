CREATE TABLE USUARIOS (
                     PK_USU integer primary key autoincrement,
                     NOME text,
                     SENHA text);
                     
CREATE TABLE TIPOS_TEMPOS (
                     PK_TTM integer primary key autoincrement,
                     DESCRICAO text,
                     INICIO_FIM text);
CREATE TABLE TIPOS_REGISTROS (
                     PK_TRG         integer primary key autoincrement,
                     DESCRICAO      text,
                     ORIGEM         text);
CREATE TABLE CORES (PK_CRS INTEGER PRIMARY KEY, DESCRICAO TEXT, PRIMARIA TEXT, SECUNDARIA TEXT, COR_TEXTO TEXT);
CREATE TABLE MOTIVOS_DIVERGENCIAS (PK_MDV integer PRIMARY KEY AUTOINCREMENT, DESCRICAO text);
CREATE TABLE MOTIVOS_PARADA (PK_MOT INTEGER PRIMARY KEY ASC AUTOINCREMENT, DESCRICAO TEXT, ATIVO TEXT, PARADA_PROGRAMADA TEXT);
CREATE TABLE TEMPOS (PK_TEM integer PRIMARY KEY AUTOINCREMENT, DATA timestamp, FK_ID integer, FK_TTM integer, FK_USU integer, MAQUINA text, FK_MOT INTEGER REFERENCES MOTIVOS_PARADA (PK_MOT), FOREIGN KEY (FK_ID) REFERENCES PDS_ANT (ID), FOREIGN KEY (FK_TTM) REFERENCES TIPOS_TEMPOS (PK_TTM), FOREIGN KEY (FK_USU) REFERENCES USUARIOS (PK_USU));
CREATE TABLE REGISTROS_QUALIDADE (PK_RGQ integer PRIMARY KEY AUTOINCREMENT, DATA integer, FK_ID integer, FK_TRG integer, VALOR_REGISTRO real, LADO text, FK_USU integer, MAQUINA text, FOREIGN KEY (FK_ID) REFERENCES PDS (PK_IRP), FOREIGN KEY (FK_TRG) REFERENCES TIPOS_REGISTROS (PK_TRG), FOREIGN KEY (FK_USU) REFERENCES USUARIOS (PK_USU));
CREATE TABLE IF NOT EXISTS "APLICAVEL" (
"ACABAMENTO" TEXT,
  "APLICAVEL" TEXT
);
CREATE TABLE IF NOT EXISTS "PDS" (
"PK_IRP" INTEGER,
  "REQUISICAO" INTEGER,
  "CELULA" TEXT,
  "DATA GERAÇÃO" INTEGER,
  "DATA ENTREGA" INTEGER,
  "OBSERVAÇÃO REQ" TEXT,
  "CHICOTE" TEXT,
  "PD" INTEGER,
  "CPD" INTEGER,
  "CABO" TEXT,
  "COR" INTEGER,
  "VIAS" TEXT,
  "BITOLA" INTEGER,
  "UNIDADE" TEXT,
  "NORMA" TEXT,
  "QTD PD REQ" INTEGER,
  "QTD_CORTADA" INTEGER,
  "MEDIDA" INTEGER,
  "DECAPE A" TEXT,
  "DECAPE B" TEXT,
  "ACABAMENTO 1" TEXT,
  "PONTE 1" TEXT,
  "ACABAMENTO 2" TEXT,
  "PONTE 2" TEXT,
  "ACABAMENTO 3" TEXT,
  "PONTE 3" TEXT,
  "ACABAMENTO 4" TEXT,
  "PONTE 4" TEXT,
  "OBSERVAÇÃO" TEXT,
  "GRAVAÇÃO" TEXT,
  "MÁQUINA" TEXT,
  "PRIORIDADE" INTEGER,
  "NR. ORDEM CORTE" INTEGER
);
